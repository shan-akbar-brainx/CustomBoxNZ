<div data-section-id="{{ section.id }}" data-section-type="brands-slider-section">
  <div id="brands-slider-{{ section.id }}" class="box_feature_brand" style="margin:{{section.settings.margin_section}};">
    <div class="container featured-brands">
      <div class="box_brand"> 
        <div class="brands-slider owl-no-narrow">
          <div class="data-carousel"  
               data-auto="{{ section.settings.autoplay }}"
               data-items="{{ section.settings.columns }}"
               data-1600="{{ section.settings.columns }}"
               data-1200="5"
               data-992="4"
               data-768="3"
               data-640="3"
               data-480="2"
               data-320="2"
               data-paging="{{ section.settings.paging }}"
               data-nav="{{ section.settings.navigation }}"
               data-margin="{{ section.settings.margin }}"
               data-prev='{{ section.settings.prev }}' 
               data-next='{{ section.settings.next }}' style="display: none;"></div>
        </div>
        <div class="owl-carousel owl-middle-nav">
          {% for block in section.blocks %}
          <div class="item">
            <a href="{{ block.settings.link }}">
              {% if block.settings.image != blank %}
              {% assign img_url = block.settings.image | img_url: '216x104' %}
              {% else %}
              {% assign img_url = '215x105.png' | asset_url %}
              {% endif %}
              <img src="{{ img_url }}" alt="{{ section.settings.image.alt }}" style="width:inherit;display: inline;"/>
            </a>
          </div>
          {% endfor %}
        </div> 
      </div>
    </div>
  </div>
</div>
{% schema %}
{
  "name": "Brands",
  "class": "index-section",
  "settings": [   
    {
      "type": "text",
      "id": "autoplay",
      "label": "Slideshow",
      "info": "Change to any integrer for example 5000 to play every 5 seconds. Leave empty to disable autoplay.",
 	  "default": "5000"
    }, 
    {
      "type": "text",
      "id": "columns",
      "label": "Columns",
      "default" : "6"
    },
    {
      "type": "text",
      "id": "margin",
      "label": "Item Margin",
	  "default": "30"
    },
    {
      "type": "checkbox",
      "id": "paging",
      "label": "Show Paging"
    },
    {
      "type": "checkbox",
      "id": "navigation",
      "label": "Show Navigation"
    },
    {
      "type": "text",
      "id": "prev",
      "label": "Prev Navigation",
      "default": "<i class=\"icon-arrow-left-products-header\"></i>"
    },
    {
      "type": "text",
      "id": "next",
      "label": "Prev Navigation",
      "default": "<i class=\"icon-arrow-right-products-header\"></i>"
    }, 
    {
      "type": "text",
      "id": "margin_section",
      "label": "Margin Section",
	  "default": "30px 0 30px 0",
      "info": "Eg: 0px 10px 20px 30px"
    }
  ],
  "blocks": [
    {
      "type": "image",
      "name": "Brand Image",
      "settings": [ 
        {
          "type": "image_picker",
          "id": "image",
          "label": "Image"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    } 
  ],
  "presets": [
    {
      "name": "Brands",
      "category": "Collection",
      "settings": {  
        "autoplay": "5000",
		"navigation": true
      }, 
      "blocks": [
        {
          "type": "image" 
        },
        {
          "type": "image" 
        },
        {
          "type": "image" 
        },
        {
          "type": "image" 
        },
        {
          "type": "image" 
        },
        {
          "type": "image" 
        },
        {
          "type": "image" 
        },
        {
          "type": "image" 
        }
      ]
    }
  ]
}
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}